{% extends 'base.html.twig' %}{% block title %}2FA deaktivieren{% endblock %}{% block body %}<p class="eyebrow">Erneute Authentifizierung</p><h1 class="h2">Zwei-Faktor-Authentisierung deaktivieren</h1><p>Dadurch wird das Konto weniger geschützt und alle bisherigen Sitzungen werden ungültig.</p>{% if error %}<div class="alert alert-danger">{{ error }}</div>{% endif %}<form method="post"><input type="hidden" name="_csrf_token" value="{{ csrf_token('totp_disable') }}"><label class="form-label" for="password">Aktuelles Passwort</label><input class="form-control mb-3" id="password" name="password" type="password" autocomplete="current-password" required autofocus><button class="btn btn-danger" type="submit">2FA deaktivieren</button></form>{% endblock %}
